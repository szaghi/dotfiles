# Read by /usr/sbin/google-chrome-stable (lines starting with # are stripped).
# Native Wayland: sharper rendering on the 1.5x-scaled DSI-1 output, and lets
# Chrome pick up the GTK theme (adw-gtk3-dark) for frame, menus and toolbar.
--ozone-platform-hint=auto
--gtk-version=4
